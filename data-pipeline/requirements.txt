# This file is a pointer, not a second manifest.
#
# The template shipped it as the "precompute lane" list, describing an EXAMPLE SIR engine that does
# not exist in this repo. Two files claiming the same lane is how a pin drifts: one gets updated and
# the other quietly does not.
#
# The real precompute manifest is ../requirements-precompute.txt. Install with:
#     pip install -r requirements-precompute.txt
-r ../requirements-precompute.txt
